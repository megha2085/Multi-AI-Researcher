[project]
name = "multi_agent_researcher"
version = "0.1.0"
description = "multi_agent_researcher using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.14.5a2"
]

[project.scripts]
multi_agent_researcher = "multi_agent_researcher.main:run"
run_crew = "multi_agent_researcher.main:run"
train = "multi_agent_researcher.main:train"
replay = "multi_agent_researcher.main:replay"
test = "multi_agent_researcher.main:test"
run_with_trigger = "multi_agent_researcher.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
